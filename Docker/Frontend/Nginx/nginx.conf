events {

}

http {
    server {
        listen 8080;
        server_name nginx;
        include /etc/nginx/mime.types;

        location /static/ {
            root /usr/share/nginx/html/;
        }
    }
}