events {

}

http {
    server {
        listen 80;
        server_name nginx;
        include /etc/nginx/mime.types;

        location /static/ {
            root /usr/share/nginx/html/;
        }
    }
}